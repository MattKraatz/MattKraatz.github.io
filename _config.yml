port: "8080"
incremental: true
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
